<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppAgendarCitas"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agregar la actividad DoctorLoginActivity -->
        <activity android:name=".DoctorLoginActivity" />

        <!-- Agregar la actividad PatientLoginActivity -->
        <activity android:name=".PatientLoginActivity" />
        <activity android:name=".DoctorRegistrationActivity" />
        <activity android:name=".PatientRegistrationActivity" />
        <activity android:name=".WelcomeActivity" />
    </application>
</manifest>
